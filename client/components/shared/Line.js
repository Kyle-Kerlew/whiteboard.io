import React from 'react';
import style from './../../styles/Line.module.css';

function Line() {
    return (
        <span className={`${style.line} container-xl`}>
        </span>
    );
}

export default Line;
